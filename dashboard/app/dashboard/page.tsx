// Placeholder screen — the guardian's list of monitored users/alerts will live here.
export default function DashboardPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-2 p-16 text-center">
      <h1 className="text-2xl font-semibold tracking-tight">Dashboard</h1>
      <p className="text-zinc-500">Placeholder page — no content yet.</p>
    </main>
  );
}
