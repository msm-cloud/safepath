// Shared with the student tab group (app/(tabs)/language.tsx) — the
// language toggle doesn't differ by role. See
// components/LanguageSettingsScreen.tsx for the actual implementation;
// this file just wires it into the (guardian) route.
export { default } from '@/components/LanguageSettingsScreen';
