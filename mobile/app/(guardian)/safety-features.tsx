// Shared with the student tab group (app/(tabs)/safety-features.tsx) —
// shake-to-trigger SOS and the fake-call escape don't differ by role. See
// components/SafetyFeaturesScreen.tsx for the actual implementation;
// this file just wires it into the (guardian) route.
export { default } from '@/components/SafetyFeaturesScreen';
