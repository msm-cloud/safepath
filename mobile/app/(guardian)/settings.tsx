// Shared with the student tab group (app/(tabs)/settings.tsx) — the
// language toggle and sign-out logic don't differ by role. See
// components/SettingsScreen.tsx for the actual implementation; this file
// just wires it into the (guardian) route.
export { default } from '@/components/SettingsScreen';
