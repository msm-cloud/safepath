// Shared with the student tab group (app/(tabs)/phone-number.tsx) — the
// phone number field doesn't differ by role. See
// components/PhoneNumberSettingsScreen.tsx for the actual implementation;
// this file just wires it into the (guardian) route.
export { default } from '@/components/PhoneNumberSettingsScreen';
