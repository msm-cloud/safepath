// Shared with the guardian tab group (app/(guardian)/phone-number.tsx) —
// the phone number field doesn't differ by role. See
// components/PhoneNumberSettingsScreen.tsx for the actual implementation;
// this file just wires it into the (tabs) route.
export { default } from '@/components/PhoneNumberSettingsScreen';
