// Shared with the guardian tab group (app/(guardian)/safety-features.tsx)
// — shake-to-trigger SOS and the fake-call escape don't differ by role.
// See components/SafetyFeaturesScreen.tsx for the actual implementation;
// this file just wires it into the (tabs) route.
export { default } from '@/components/SafetyFeaturesScreen';
