// Shared with the guardian tab group (app/(guardian)/language.tsx) —
// the language toggle doesn't differ by role. See
// components/LanguageSettingsScreen.tsx for the actual implementation;
// this file just wires it into the (tabs) route.
export { default } from '@/components/LanguageSettingsScreen';
