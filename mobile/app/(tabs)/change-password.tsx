// Shared with the guardian tab group (app/(guardian)/change-password.tsx)
// — changing a password doesn't differ by role. See
// components/ChangePasswordScreen.tsx for the actual implementation;
// this file just wires it into the (tabs) route.
export { default } from '@/components/ChangePasswordScreen';
