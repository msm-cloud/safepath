// Shared with the guardian tab group (app/(guardian)/tutorial.tsx) — see
// components/HelpTutorialScreen.tsx for the actual implementation; this
// file just wires it into the (tabs) route.
export { default } from '@/components/HelpTutorialScreen';
